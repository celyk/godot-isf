[gd_resource type="ShaderMaterial" load_steps=2 format=3 uid="uid://cmagfpnocdt2s"]

[sub_resource type="Shader" id="Shader_b4ht5"]
code = "shader_type canvas_item;

void vertex() {
	// Called for every vertex the material is visible on.
}

void fragment() {
	COLOR = vec4(0,0,0,1);
	COLOR.rg = UV;
}

//void light() {
//	// Called for every pixel for every light affecting the material.
//	// Uncomment to replace the default light processing function with this one.
//}
"

[resource]
shader = SubResource("Shader_b4ht5")
